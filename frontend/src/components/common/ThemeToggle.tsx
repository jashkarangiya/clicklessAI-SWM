'use client';
/**
 * ClickLess AI – ThemeToggle (no-op)
 * Dark mode removed from the product — component retained so imports compile.
 */
export function ThemeToggle({ size = 20 }: { size?: number }) {
  return null;
}
